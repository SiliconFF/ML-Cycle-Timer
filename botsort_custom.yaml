tracker_type: botsort
track_high_thresh: 0.5
track_low_thresh: 0.1
new_track_thresh: 0.6
track_buffer: 100      # How many frames to remember a lost tool
match_thresh: 0.8
fuse_score: True       # Fuses detection score with IOU
# --- NEW REQUIRED FIELDS ---
gmc_method: sparseOptFlow # Helps track if the camera shakes
proximity_thresh: 0.5
appearance_thresh: 0.25
with_reid: True
model: auto            # Automatically uses your model features for ID